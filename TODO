- Make elements and element properties not rely on `activeId` when modifying the element
- Element layer system & better UI for element properties
- Add support for adding svgs over image such as material svg pack
